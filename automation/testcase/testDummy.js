var chai = require('chai');
var assert = chai.assert;

describe('testDummy -- this Node.js mocha + chai test environment works', function() {
  it('Dummy works', function() {
    assert.isOk(true, "Hello World!");
  });
});
